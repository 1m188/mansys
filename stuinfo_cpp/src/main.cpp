#include "iostream"
#include "Windows.h"
#include "utility.h"

int main(int argc, char *argv[])
{
    SetConsoleTitle("Student Information Manage System");
    displayCursor(false);

    std::cout << "Hello world\n";
    while (true)
    {
    }

    return 0;
}